#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    long long int M, N;
    scanf("%lld %lld", &M, &N);

    long long Result = M * N;

    printf("%lld", Result);
    return 0;
}
